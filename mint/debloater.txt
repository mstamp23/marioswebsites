sudo apt purge --autoremove -y \
  celluloid hypnotix rhythmbox \
  libreoffice-core libreoffice-calc libreoffice-draw libreoffice-impress libreoffice-writer \
  libreoffice-base-core libreoffice-common \
  gnome-dictionary xfce4-dict \
  zim evince nheko \
  transmission-gtk transmission-common \
  thunderbird thunderbird-locale-* \
  webapp-manager simple-scan drawing \
  pix eog xviewer \
  mintwelcome mintreport \
  gnome-calendar gnome-calculator xreader \
  && sudo apt clean

disable from Autostart
for f in blueman geoclue-demo-agent nvidia-prime nvidia-settings-autostart onboard-autostart orca-autostart org.gnome.Evolution-alarm-notify print-applet light-locker xiccd; do
  cp /etc/xdg/autostart/${f}.desktop ~/.config/autostart/
  echo "Hidden=true" >> ~/.config/autostart/${f}.desktop
done
